import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javaquiz.app',
  appName: 'java-quiz',
  webDir: 'dist/java-quiz/browser'
};

export default config;
